{
  "name": "go-pilot",
  "displayName": "Go-Pilot",
  "description": "View, search and browse backed-up Copilot chat sessions",
  "version": "0.0.3",
  "publisher": "local-dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/asos-dominicjomaa/co-copilot-pops"
  },
  "icon": "media/icon-marketplace.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "scripts": {
    "test": "jest",
    "package": "vsce package --no-dependencies --allow-missing-repository --skip-license",
    "install-extension": ". $HOME/.nvm/nvm.sh && nvm use 20 && vsce package --no-dependencies --allow-missing-repository --skip-license && code --install-extension $(node -p \"require('./package.json').name + '-' + require('./package.json').version + '.vsix'\")"
  },
  "devDependencies": {
    "@vscode/vsce": "^2.32.0",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1"
  },
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilot-chat-viewer.open",
        "title": "Go-Pilot: Open"
      },
      {
        "command": "copilot-chat-viewer.dev.installAndReload",
        "title": "Go-Pilot: Install Extension and Reload Window"
      }
    ],
    "keybindings": [
      {
        "command": "copilot-chat-viewer.dev.installAndReload",
        "key": "ctrl+alt+r",
        "mac": "cmd+alt+r",
        "when": "isWorkspaceTrusted"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "go-pilot",
          "title": "Go-Pilot",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "go-pilot": [
        {
          "type": "webview",
          "id": "copilotChatViewerSidebar",
          "name": "Workspaces"
        }
      ]
    },
    "configuration": {
      "title": "Go-Pilot",
      "properties": {
        "copilotChatViewer.backupFolder": {
          "type": "string",
          "default": "",
          "description": "Path to the vscode-chats-backup folder."
        }
      }
    }
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}